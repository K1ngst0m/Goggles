# Goggles Configuration File
# Format: TOML (https://toml.io/)

# =============================================================================
# Capture Settings
# =============================================================================
[capture]
# Backend to use for frame capture
# Options: "vulkan_layer" (primary), "compositor" (fallback)
backend = "vulkan_layer"

# =============================================================================
# Shader Settings
# =============================================================================
[shader]
# Path to shader preset file (.slangp)
# Leave empty for passthrough mode (no processing)
preset = "shaders/retroarch/crt/zfast-crt.slangp"

# =============================================================================
# Render Settings
# =============================================================================
[render]
# Enable VSync (synchronize with display refresh rate)
vsync = true

# Target frame rate (frames per second)
# Only applies when vsync = false
target_fps = 60

# Enable Vulkan validation layers for debugging
# Set to true to enable VK_LAYER_KHRONOS_validation (requires Vulkan SDK)
enable_validation = true

# Display scaling mode for captured frames
# Options:
#   "fit"     - Scale to fit within window, preserving aspect ratio (letterbox/pillarbox)
#   "fill"    - Scale to fill window, preserving aspect ratio (may crop edges)
#   "stretch" - Stretch to fill window exactly, ignoring aspect ratio
#   "integer" - Pixel-perfect integer scaling (uses integer_scale setting)
scale_mode = "integer"

# Integer scaling multiplier (only used when scale_mode = "integer")
# 0 = auto (maximum integer scale that fits in window)
# 1 = original size (no scaling)
# 2-8 = fixed multiplier (e.g., 2 = 2x, 3 = 3x)
integer_scale = 3

# =============================================================================
# Logging Settings
# =============================================================================
[logging]
# Log level threshold
# Options: "trace", "debug", "info", "warn", "error", "critical"
# Note: trace/debug are disabled in release builds by default
level = "info"

# Log file path (empty = console output only)
# Example: "logs/goggles.log"
file = ""
