# Valid test configuration file

[capture]
backend = "vulkan_layer"

[shader]
preset = "shaders/test.glsl"

[render]
vsync = false
target_fps = 120
gpu_selector = "AMD"

[logging]
level = "debug"
file = "test.log"
