name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Cache CPM dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/CPM
        key: ${{ runner.os }}-cpm-${{ hashFiles('cmake/Dependencies.cmake') }}
        restore-keys: |
          ${{ runner.os }}-cpm-

    - name: Cache apt packages
      uses: actions/cache@v4
      with:
        path: /var/cache/apt/archives
        key: ${{ runner.os }}-apt-${{ hashFiles('.github/workflows/ci.yml') }}
        restore-keys: |
          ${{ runner.os }}-apt-

    - name: Install Vulkan SDK
      run: |
        wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-noble.list https://packages.lunarg.com/vulkan/lunarg-vulkan-noble.list
        sudo apt-get update
        sudo apt-get install -y vulkan-sdk

    - name: Install dependencies
      run: |
        sudo apt-get install -y cmake ninja-build ccache \
          libx11-dev libxext-dev libxrandr-dev libxi-dev libxcursor-dev libxss-dev

    - name: Setup ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ${{ runner.os }}-ccache-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-ccache-

    - name: Configure ccache
      run: |
        ccache --set-config=max_size=500M
        ccache --set-config=compression=true
        ccache --zero-stats

    - name: Configure with ASAN preset
      run: cmake --preset asan

    - name: Build
      run: cmake --build --preset asan

    - name: Show ccache statistics
      run: ccache --show-stats

    - name: Run Tests
      run: ctest --preset asan --output-on-failure

  static-analysis:
    name: Static Analysis (clang-tidy)
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Cache CPM dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/CPM
        key: ${{ runner.os }}-cpm-${{ hashFiles('cmake/Dependencies.cmake') }}
        restore-keys: |
          ${{ runner.os }}-cpm-

    - name: Cache apt packages
      uses: actions/cache@v4
      with:
        path: /var/cache/apt/archives
        key: ${{ runner.os }}-apt-${{ hashFiles('.github/workflows/ci.yml') }}
        restore-keys: |
          ${{ runner.os }}-apt-

    - name: Install Vulkan SDK
      run: |
        wget -qO- https://packages.lunarg.com/lunarg-signing-key-pub.asc | sudo tee /etc/apt/trusted.gpg.d/lunarg.asc
        sudo wget -qO /etc/apt/sources.list.d/lunarg-vulkan-noble.list https://packages.lunarg.com/vulkan/lunarg-vulkan-noble.list
        sudo apt-get update
        sudo apt-get install -y vulkan-sdk

    - name: Install dependencies
      run: |
        sudo apt-get install -y cmake clang-tidy ninja-build ccache \
          libx11-dev libxext-dev libxrandr-dev libxi-dev libxcursor-dev libxss-dev

    - name: Setup ccache
      uses: actions/cache@v4
      with:
        path: ~/.ccache
        key: ${{ runner.os }}-ccache-tidy-${{ github.sha }}
        restore-keys: |
          ${{ runner.os }}-ccache-tidy-

    - name: Configure ccache
      run: |
        ccache --set-config=max_size=500M
        ccache --set-config=compression=true
        ccache --zero-stats

    - name: Configure with Test preset (includes clang-tidy)
      run: cmake --preset test

    - name: Build with clang-tidy
      run: cmake --build --preset test

    - name: Show ccache statistics
      run: ccache --show-stats

  openspec-check:
    name: OpenSpec Validation
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
    - uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install openspec
      run: npm install -g @fission-ai/openspec

    - name: Run OpenSpec validation
      id: openspec
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        COMMENT_BODY=""
        HAS_ERRORS=false

        # 1. Validate all proposals
        echo "::group::Validating proposals..."
        if ! validation_output=$(openspec validate --all --strict 2>&1); then
          HAS_ERRORS=true
          COMMENT_BODY+="### ❌ Proposal Validation Failed

        The following proposals have validation errors:

        \`\`\`
        $validation_output
        \`\`\`

        **How to fix:**
        1. Run \`openspec validate <change-id> --strict\` locally to see detailed errors
        2. Common issues:
           - Missing \`#### Scenario:\` headers (must use 4 hashtags)
           - Requirements without scenarios
           - Invalid delta operations (use \`## ADDED/MODIFIED/REMOVED Requirements\`)
        3. See \`openspec/AGENTS.md\` for spec format reference

        ---
        "
        fi
        echo "::endgroup::"

        # 2. Check task completion (proposals with X/Y where X < Y)
        echo "::group::Checking task completion..."
        incomplete=$(openspec list 2>/dev/null | grep -E '[0-9]+/[0-9]+ tasks' | awk '{split($2,a,"/"); if(a[1]<a[2]) print $1, a[1], a[2]}')
        incomplete_tasks=""

        if [ -n "$incomplete" ]; then
          while read -r change_id completed total; do
            remaining=$((total - completed))
            incomplete_tasks+="| \`$change_id\` | $completed | $total | $remaining |
        "
          done <<< "$incomplete"

          HAS_ERRORS=true
          COMMENT_BODY+="### ❌ Incomplete Tasks Found

        The following proposals have incomplete tasks:

        | Proposal | Completed | Total | Remaining |
        |----------|-----------|-------|-----------|
        $incomplete_tasks

        **How to fix:**
        1. Open \`openspec/changes/<proposal-id>/tasks.md\`
        2. Complete all tasks marked with \`- [ ]\`
        3. Mark completed tasks with \`- [x]\`
        4. If tasks are no longer needed, remove them from the list

        ---
        "
        fi
        echo "::endgroup::"

        # 3. Check unarchived completed proposals (shows "Complete" in output)
        echo "::group::Checking for unarchived proposals..."
        completed_proposals=$(openspec list 2>/dev/null | grep "Complete" | awk '{print $1}')

        if [ -n "$completed_proposals" ]; then
          unarchived=""
          while read -r change_id; do
            unarchived+="- \`$change_id\`
        "
          done <<< "$completed_proposals"

          HAS_ERRORS=true
          COMMENT_BODY+="### ❌ Unarchived Completed Proposals

        The following proposals have all tasks complete but are not archived:

        $unarchived

        **How to fix:**
        1. Run \`openspec archive <change-id> --yes\` to archive the proposal
        2. This will move the proposal to \`openspec/changes/archive/\`
        3. Include the archived proposal in this PR

        ---
        "
        fi
        echo "::endgroup::"

        # Post comment if there are errors (PR only)
        if [ "$HAS_ERRORS" = true ] && [ "${{ github.event_name }}" = "pull_request" ]; then
          # Write comment to temp file to avoid quoting issues
          {
            echo "## 🔍 OpenSpec Validation Report"
            echo ""
            echo "$COMMENT_BODY"
            echo "<details>"
            echo "<summary>📚 Quick Reference</summary>"
            echo ""
            echo '```bash'
            echo "# Validate a specific proposal"
            echo "openspec validate <change-id> --strict"
            echo ""
            echo "# List all active proposals"
            echo "openspec list"
            echo ""
            echo "# Archive a completed proposal"
            echo "openspec archive <change-id> --yes"
            echo ""
            echo "# View proposal details"
            echo "openspec show <change-id>"
            echo '```'
            echo ""
            echo "</details>"
          } > /tmp/pr_comment.md

          gh pr comment ${{ github.event.pull_request.number }} --body-file /tmp/pr_comment.md || echo "::warning::Failed to post PR comment"
        fi

        # Fail the job if there were errors
        if [ "$HAS_ERRORS" = true ]; then
          echo "::error::OpenSpec validation failed. See PR comment for details."
          exit 1
        fi

        echo "✅ All OpenSpec checks passed!"
