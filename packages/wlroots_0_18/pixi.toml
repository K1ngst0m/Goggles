[package]
name = "wlroots-0-18"
version = "0.18.3"
description = "wlroots 0.18.x compositor library (prebuilt, headless backend + XWayland)"
authors = ["Goggles Team"]
license = "MIT"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "==0.3.5" }
